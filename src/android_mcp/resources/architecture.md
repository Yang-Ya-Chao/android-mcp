# Android 架构与源码依据

实现前先检索 Google 官方架构建议、AndroidX 源码和项目现有模块边界。优先保持单向数据流、生命周期感知、可测试的业务层和清晰的依赖方向；不要为了短期修复把平台调用、状态和持久化逻辑无边界地塞进 UI 层。

架构选择必须说明适用的 API level、线程模型、生命周期和错误处理。若官方文档与项目现有实现不同，先通过项目测试和目标设备验证，再记录兼容策略；不要只凭模型记忆更换框架或 API。
